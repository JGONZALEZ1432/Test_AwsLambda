service: Pricing-Vehicles

useDotenv: true

provider:
  name: aws
  runtime: nodejs14.x

functions:
  finance:
    handler: src/interfaces/http/book-router.handler

custom:
  esbuild:
    bundle: true
    minify: true

plugins:
  - serverless-esbuild
  - serverless-offline
