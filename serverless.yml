service: my-lambda-function
provider:
  name: aws
  runtime: nodejs14.x

functions:
  createUser:
    handler: dist/interfaces/handler.createUserHandler
    events:
      - http:
          path: /users
          method: post
  getUser:
    handler: dist/interfaces/handler.getUserHandler
    events:
      - http:
          path: /users/{id}
          method: get
  updateUser:
    handler: dist/interfaces/handler.updateUserHandler
    events:
      - http:
          path: /users/{id}
          method: put
  deleteUser:
    handler: dist/interfaces/handler.deleteUserHandler
    events:
      - http:
          path: /users/{id}
          method: delete

package:
  include:
    - dist/**
